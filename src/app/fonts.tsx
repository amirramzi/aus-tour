import { Inter, Antonio } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const antonio = Antonio({
  subsets: ["latin"],
  display: "swap",
});
